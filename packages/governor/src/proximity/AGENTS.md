# Proximity Domain

This domain handles spatial relationships and proximity calculations.

> **Note:** ProximityManager has been refactored to ProximityService in `@minimal-rpg/state-manager`. This domain may be deprecated or used for governor-specific proximity logic in the future.

## Future Implementation

- `proximity-manager.ts`: Governor-specific proximity handling (if needed)
