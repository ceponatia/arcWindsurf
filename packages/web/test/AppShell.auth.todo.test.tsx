import { describe, it } from 'vitest';

describe('AppShell auth integration', () => {
  it.todo(
    'wires Supabase auth state via useAuth to gate session access and startup (dev-docs/000-future-implementations/001-appshell-supabase-auth.md)'
  );
});
