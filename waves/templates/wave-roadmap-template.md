# Wave x.x Roadmap: Title

This wave { description }

## Completion Order

> **Note to Agent:** Upon completion of each task, you must update the `status` field in the corresponding `.yaml` task file to `completed` and add any relevant implementation notes or discovered constraints to the file.

1. { hyperlink reference documents (YAML files) }

---

## Agent Prompts

### Task: { First Task}

**Prompt:**
> Please implement { hyperlink task }. Upon completion, update the task's status to `completed` in its .yaml file and add any implementation notes.
> **Mandatory Reading:** { Add context docs }
> **Context:** { ex: Refactor the top-level state to include a `List<Sheet>`. Migrate all existing floating nodes into a default "Legacy" sheet to maintain data integrity. }
